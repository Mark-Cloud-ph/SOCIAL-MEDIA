{
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && nodemon app.js",
    "frontend": "cd Home && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "zustand": "^5.0.2"
  }
}
